spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888
      name: user-service,postgres-datasource # config server의 application 이름을 적어주면 [application-name].yml 파일을 읽어온다.

---

#spring:
#  config:
#    activate:
#      on-profile: dev
#  cloud:
#    config:
#      uri: http://config-service:8888
#  profiles:
#    active: dev